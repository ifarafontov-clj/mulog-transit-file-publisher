(ns ifarafontov.transit-publisher-test
  (:require [clojure.test :refer :all]
            [ifarafontov.transit-publisher :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
